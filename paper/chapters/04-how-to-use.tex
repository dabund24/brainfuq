\section{How to use our Code}

Include important code snippets

\begin{lstlisting}[language=Python, caption=Example Python code]
def example_function(x, y):
    """
    Brief description of what this function does.
    
    Args:
        x: First parameter
        y: Second parameter
    
    Returns:
        Result of the computation
    """
    result = x + y
    return result
\end{lstlisting}

such that we know how to use your code.

